# Documentação interna — não publicar
PROJECT-README-INTERNAL.md

# Assets grandes (brandbook PDF — 803MB, manter local)
BRANDBOOK/

# Materiais de identidade Mother's Team (LAYOUT APP.pdf ~170MB + logos), manter local
docs/materials/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-android
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env

# Visual brainstorming sessions (superpowers plugin)
.superpowers/

# Temporários de brainstorm (renders de PDF, screenshots de teste)
.tmp-materials/
.playwright-mcp/

# Server
server/.env
server/dist/
.claude/worktrees/

# ElevenLabs migration workspace (contains API keys hardcoded)
.migrate-tmp/

# Credenciais Firebase Admin SDK (chave privada — nunca commitar)
*-firebase-adminsdk-*.json
*.firebase-b64.tmp

# Certificados e chaves de distribuição iOS (nunca commitar)
*.key
*.csr
*.cer
*.p12
*.mobileprovision

# Arquivos temporários locais
presentation.html
check-logs.py
image.png
Screenshot_*.png
feature-graphic-1024x500.png
icon-playstore-512.png
